#!/bin/bash
bssid=''
bssid=$(nmcli device wifi list | sed -n '1!p' | cut -b 9- | bemenu_config -p "select wifi 󰣸 : " -l 20 | cut -d' ' -f1)
if [ $bssid ='' ]; then
else
	pass=$(echo ""|bemenu_config -p -x "Enter password 󰌋 :")
	nmcli device wifi connect $bssid password $pass
fi

